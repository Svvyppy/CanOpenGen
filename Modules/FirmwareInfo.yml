schema: 1

module:
  name: Firmware Information
  info: |
    Common firmware identification objects.

modules:
  - CommonTypes

objects:
  firmware_version:
    category: diagnostic
    type: uint32
    access: ro
    info: |
      Packed firmware version.
