# Modules

Reusable module definitions live under `Modules/`. Module filenames define their
namespaces. Recursive loading, cycle detection, and reference resolution are planned
for the module-resolution phase.

