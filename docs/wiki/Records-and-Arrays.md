# Records and Arrays

Records will reserve subindex `0x00` and allocate fields deterministically within
`0x01` through `0xFE`. Arrays will use standard sequential CANopen subindices. Full
examples will accompany their implementation.

