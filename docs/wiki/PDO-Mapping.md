# PDO Mapping

CanOpenGen will resolve symbolic TPDO and RPDO entries, calculate mapping values, and
reject classic CANopen payloads above 64 bits. Advanced communication parameters are
outside the MVP scope.

