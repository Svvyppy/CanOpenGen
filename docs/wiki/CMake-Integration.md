# CMake Integration

The MVP will expose `canopen_device(...)` through add-subdirectory integration. All
generated artifacts will remain below the configured build output directory and YAML
dependencies will participate in the build graph.

