# YAML Reference

Schema version 1 will define device metadata, modules, custom types, objects, records,
arrays, and PDO mappings. This reference will be synchronized with the parser and JSON
Schema as those features are implemented.

