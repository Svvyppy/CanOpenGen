# Getting Started

The installation, first device, and generation tutorial will be completed alongside
the CLI and end-to-end generation pipeline. Until then, use the development setup in
the repository README to run the bootstrap checks.

