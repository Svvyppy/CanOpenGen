# CLI Reference

The planned CLI commands are `validate`, `validate-all`, `generate`, `map`, and
`address`. Their final arguments, output, and exit-status behavior will be documented
when the CLI is implemented.

